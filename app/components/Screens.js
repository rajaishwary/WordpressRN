import React, { Component } from 'react';
import {
    Text,
} from 'react-native';
import TabBarContainer from '../common/TabBarContainer';

export default class Screens extends Component {
    render() {
        return (
            <TabBarContainer>
                <Text>YEYYYYYYYYYYYY</Text>
            </TabBarContainer>
        );
    }
}